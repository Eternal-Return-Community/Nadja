import { randomUUID } from 'crypto'

export const GAME_ID = 1049590;
export const GAME_VERSION = '1.24.0'
export const BASE_URL = 'https://bser-rest-release.bser.io/api'
export const DEVICE = randomUUID().replace('-', '');